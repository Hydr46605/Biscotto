import type { ModuleManifest } from '../../contracts/module.contract.ts';

export const manifest: ModuleManifest = {
  name: 'dashboard',
  version: '1.0.0',
  description: 'User profile dashboard with Components V2',
} as const;
